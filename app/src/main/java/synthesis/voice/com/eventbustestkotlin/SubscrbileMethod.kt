package synthesis.voice.com.eventbustestkotlin

import java.lang.reflect.Method

class SubscrbileMethod (mMeath : Method,mThreadMod : ThreadMode,type: Class<*>){
    var mMeath : Method ?= mMeath
    var mThreadMod : ThreadMode? = mThreadMod
    var type: Class<*> ? = type


}